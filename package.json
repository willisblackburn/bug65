{
  "name": "bug65-vscode-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/bug65-core",
    "packages/vscode-extension",
    "packages/bug65-cli"
  ],
  "scripts": {
    "test": "npm test --workspaces",
    "compile": "tsc -b",
    "watch": "tsc -b -w"
  },
  "devDependencies": {
    "@types/node": "^25.0.6",
    "@types/vscode": "^1.108.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "vscode-debugadapter": "^1.51.0",
    "vscode-debugprotocol": "^1.51.0"
  }
}